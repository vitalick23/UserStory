@model List<UserStore.BLL.Entities.Comment>

<div id="searchresults">
    <h3></h3>
    <ul>
        @foreach (var item in Model)
        {
            <li>Name: @item.User.Email</li>
            <li>Time: @item.TimePublicate</li>
            <li>Comment: @item.Text</li>
        }
    </ul>
</div>
